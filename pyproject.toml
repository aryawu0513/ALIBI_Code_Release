[project]
name = "alibi-artifact"
version = "0.1.0"
description = "ALIBI adaptive-attack and defense code"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0",
    "PyYAML>=6.0",
]

[project.optional-dependencies]
anthropic = ["anthropic>=0.40"]

[tool.uv]
package = false
